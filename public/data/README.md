# Data Folder

`territories.json` se genera a partir de los KML de `/kmlfiles` con
`npm run import-kml` (`scripts/import-kmls.js`). `npm run build` lo regenera
siempre antes de compilar, así que no hace falta editarlo a mano.
